import Layout from '../components/MyLayout'

const style = {
    fontSize: 50,
    textAlign: "center"
};

export default () => (
    <Layout>
        <p style={style}>LETS TALK PAGE!</p>
    </Layout>
)
